🔧 TASK: Fix Agent Execution Logic (Balanced Version)

-------------------------------------
🎯 الهدف:
- تنفيذ مباشر بدون تردد
- منع loop
- الحفاظ على DOM كـ دعم وليس عائق

=====================================
[ 1 ] SEARCH → EXECUTE (الأساس)

إذا search_text وجد ملف:

→ مباشرة:
read_file
→ edit_component

❌ بدون:
browse_page
get_page_structure
screenshot_page

-------------------------------------

if (searchFoundFile === true) {
  skipDOM = true;
}

=====================================
[ 2 ] DOM = OPTIONAL (ليس شرط)

❌ احذف أي شرط مثل:

if (!DOM) → block

-------------------------------------

✔ استبدله:

if (DOM available) {
  use it as context only
}

-------------------------------------

🎯 DOM = تحسين الفهم فقط

=====================================
[ 3 ] منع LOOP

❌ لا يسمح:

DOM_INSPECTION_REQUIRED (loop)

-------------------------------------

✔ التعديل:

MAX_DOM_BLOCKS = 1

-------------------------------------

إذا فشل DOM مرة:

→ تجاوزه مباشرة

=====================================
[ 4 ] READ → EDIT مباشرة

إذا تم:

read_file

❌ ممنوع:
- search مرة أخرى
- DOM
- browse_page

-------------------------------------

✔ مباشرة:

edit_component

=====================================
[ 5 ] screenshot logic (مهم)

❌ ممنوع screenshot قبل التعديل

-------------------------------------

✔ يسمح فقط:

بعد edit_component
أو إذا فشل التعديل

-------------------------------------

if (editSuccess) {
  optional screenshot
}

=====================================
[ 6 ] منع أدوات المتصفح عند وجود target

إذا:

targetState.found === true

❌ امنع:

browse_page
screenshot_page
get_page_structure

-------------------------------------

=====================================
[ 7 ] دعم العصا السحرية (DOM user input)

إذا المستخدم أعطى DOM:

→ اعتبره كافي 100%

❌ لا تعمل browse_page إضافي

✔ مباشرة:
edit_component

=====================================
[ 8 ] قاعدة التنفيذ النهائية

Priority:

1. search_text
2. read_file
3. edit_component
4. (اختياري) screenshot

-------------------------------------

❌ لا تقف  
❌ لا تعيد التفكير  
✔ نفّذ مباشرة

=====================================
🎯 النتيجة:

- لا يوجد DOM blocking
- لا يوجد loop
- التنفيذ سريع
- DOM موجود كدعم فقط